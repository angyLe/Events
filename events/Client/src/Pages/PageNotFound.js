import React from "react";
import tr from "../Utils/translationHelper";

const PageNotFound = () => {
  return (
    <div className="pageNotFound">{tr("PageNotFound", "Page not Found")}</div>
  );
};

export default PageNotFound;
