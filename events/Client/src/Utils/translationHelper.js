/*  Translates words and phrases

    TODO: function is not ready it just returns default value.
    wordToTranslate cannot be used as default value 
    because some solutions do not support phrases.
    
    TODO: add test, when function is ready to use
*/
const translationHelper = (wordToTranslate, defaultValue) => defaultValue;

const tr = translationHelper;
export default tr;
